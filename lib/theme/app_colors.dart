import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // supaya tidak bisa diinstansiasi

  // Primary Colors
  static const Color primary = Color(0xFF7BC96F); // soft green
  static const Color accent = Color(0xFFFFB36A); // soft orange

  // Background
  static const Color background = Color(0xFFF6F8FC);

  // Text
  static const Color textPrimary = Color.fromARGB(255, 69, 67, 67);
  static const Color textSecondary = Color(0xFF8A8A8A);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);

  // Border & Divider
  static const Color border = Color(0xFFE0E0E0);
}
